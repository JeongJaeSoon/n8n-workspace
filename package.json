{
  "name": "n8n-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "n8n Custom Node Mono-repo for Internal Mode",
  "workspaces": [
    "packages/*",
    "packages/nodes/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test",
    "new:node": "./scripts/new-node.sh"
  }
}
